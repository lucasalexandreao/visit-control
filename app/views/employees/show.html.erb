<div class="container d-flex justify-content-center mt-5">
  <div class="card p-4" style="max-width: 600px; width: 100%; position: relative;">
    <div class="card-body">
      <% if notice.present? %>
        <p class="text-success text-center"><%= notice %></p>
      <% end %>

      <%= render @employee %>

      <div class="mt-4 d-flex justify-content-center gap-2">
        <%= link_to "Editar", edit_employee_path(@employee), class: "btn btn-primary" %>
        <%= link_to "Voltar para funcionários", employees_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>
