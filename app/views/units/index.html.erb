<p style="color: green"><%= notice %></p>

<% content_for :title, "Units" %>

<h1>Units</h1>
<%= link_to "Cadastrar unidade", new_unit_path %>

<div id="units">
  <% @units.each do |unit| %>
    <%= render unit %>
    <p>
      <%= link_to "Show this unit", unit %>
    </p>
  <% end %>
</div>

